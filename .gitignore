# Vault password (NEVER commit this!)
.vault_password

# Downloaded binaries (installed by setup.sh)
scripts/fzf.exe
scripts/fzf
scripts/direnv.exe
scripts/direnv

# SSH keys
*.ppk
id_rsa*
id_ed25519*

# Local overrides
*.local

# OS files
.DS_Store
Thumbs.db

# Backup files
*.bak
*.backup
*~
./claude/settings.local.json
# Note: Encrypted secrets (VAULT_ENCRYPTED) ARE safe to commit
# Only unencrypted secrets should never be committed
